'use strict';

/**
 *  warehouse-catalogue controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::warehouse-catalogue.warehouse-catalogue');
